import React from 'react';
import classes from './BuildControls.module.css';

const buildControls = (props) => {
  return <div className={classes.BuildControls}>build controls area</div>;
};

export default buildControls;
